#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ionic2-custom-icons/npm-script/bin/ionic2-custom-icons.js" "$@"
  ret=$?
else 
  node  "$basedir/../ionic2-custom-icons/npm-script/bin/ionic2-custom-icons.js" "$@"
  ret=$?
fi
exit $ret
