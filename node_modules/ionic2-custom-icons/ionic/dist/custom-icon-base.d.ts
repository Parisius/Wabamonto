import { Config } from 'ionic-angular';
import { ClassUpdater } from './util/ClassUpdater';
/**
 * Base class for custom-icon directives. Mainly responsible for setting/updating the target element's CSS class.
 *
 * @author Gerrit Erpenstein
 */
export declare abstract class CustomIconBase {
    /**
     * CSS set prefix
     */
    private static readonly SET_PREFIX;
    /**
     * CSS icon prefix
     */
    private static readonly ICON_PREFIX;
    /**
     * Platform mode (md, ios or wp)
     */
    private _mode;
    /**
     * Stores currently set classes
     */
    private _classes;
    /**
     * Promise that resolves to the CSS class updater instance
     */
    private _classUpdaterPromise;
    private _classUpdaterResolve;
    constructor(config: Config);
    /**
     * Set ClassUpdater instance.
     * Note: Setting the ClassUpdater is mandatory!
     * @param classUpdater
     */
    protected classUpdater: ClassUpdater;
    /**
     * Update icon name.
     * @param icon
     */
    protected updateIcon(icon: string): void;
    /**
     * Update set name.
     * @param set
     */
    protected updateSet(set: string): void;
    /**
     * Update platform mode.
     * @param mode md, ios or wp
     */
    protected updateMode(mode: string): void;
    /**
     * Update icon color.
     * @param color
     */
    protected updateColor(color: string): void;
    /**
     * Update icon to active/inactive.
     * @param active
     */
    protected updateActive(active: boolean): void;
    /**
     * Update class (key) to given value
     * @param key
     * @param value
     * @private
     */
    private _updateClass(key, value);
    /**
     * Remove all CSS class values from this element set by this class.
     */
    protected removeElementClasses(): void;
}
