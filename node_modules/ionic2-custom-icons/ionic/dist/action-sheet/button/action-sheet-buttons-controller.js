import { ActionSheetButtonCustomIcon } from './action-sheet-button-custom-icon';
/**
 * Helper controller for adding/removing custom icons to the buttons in an action sheet.
 *
 * @author Gerrit Erpenstein
 */
var ActionSheetButtonsController = (function () {
    function ActionSheetButtonsController(_actionSheet, _buttonOptions, _config) {
        this._actionSheet = _actionSheet;
        this._buttonOptions = _buttonOptions;
        this._config = _config;
        this._icons = [];
    }
    /**
     * Initially get and set the ElementRef instance of the action sheet.
     * @private
     */
    ActionSheetButtonsController.prototype._initElementRef = function () {
        if (!this._elementRef) {
            this._elementRef = this._actionSheet.pageRef();
        }
    };
    /**
     * Add/set the custom icons for the action sheet instance.
     */
    ActionSheetButtonsController.prototype.addCustomIcons = function () {
        this._initElementRef();
        var buttons = this._elementRef.nativeElement.querySelectorAll('button');
        // Set custom icon for each button
        for (var i = 0; i < buttons.length; i++) {
            if (this._buttonOptions[i].hasOwnProperty('customIcon')) {
                var customIcon = new ActionSheetButtonCustomIcon(this._buttonOptions[i].customIcon, buttons[i], this._config);
                customIcon.render();
                this._icons.push(customIcon);
            }
        }
    };
    /**
     * Remove the custom icons of the action sheet instance.
     */
    ActionSheetButtonsController.prototype.removeCustomIcons = function () {
        for (var _i = 0, _a = this._icons; _i < _a.length; _i++) {
            var icon = _a[_i];
            icon.destroy();
        }
        this._icons = [];
    };
    return ActionSheetButtonsController;
}());
export { ActionSheetButtonsController };
//# sourceMappingURL=action-sheet-buttons-controller.js.map