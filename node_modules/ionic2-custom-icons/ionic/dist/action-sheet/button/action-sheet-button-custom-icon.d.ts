import { Config } from 'ionic-angular';
import { CustomIconBase } from '../../custom-icon-base';
import { ActionSheetButtonCustomIconOptions } from '../action-sheet-custom-icon-options';
/**
 * Renders a custom icon into an action sheet button.
 *
 * @author Gerrit Erpenstein
 */
export declare class ActionSheetButtonCustomIcon extends CustomIconBase {
    private _options;
    private _buttonElement;
    private _iconElement;
    constructor(_options: ActionSheetButtonCustomIconOptions, _buttonElement: HTMLElement, config: Config);
    /**
     * Render the custom icon.
     */
    render(): void;
    private _updateOptions(options?);
    /**
     * Destroy/remove the custom icon.
     */
    destroy(): void;
}
